(() => {
    'use strict';

    const config = {
        server: {
            ip: 'localhost',
            port: 8080
        },
        sendgrid: {
            apikey: ''
        }
    };

    module.exports = config;
})();
